/**
 * README
 * ------
 */

/**
 * v1.0.3 - Added checked for files since deleted from GDrive, as it used 
 * to error if it tried to then create a shortcut
 */

/**
 * v1.0.2 - Added check for fileId in properties. Not stored if file to 
 * big to download
 */

/**
 * v1.0.1 - Removed "reset" from menu (bit dangerour)
 */